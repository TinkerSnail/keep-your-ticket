schema_version = "1.0.0"

id = "kyt_tools"
version = "0.1.0"
name = "Keep Your Ticket"
tagline = "Check a prop and send it to the game"
maintainer = "Keep Your Ticket"
type = "add-on"

blender_version_min = "4.2.0"

license = ["SPDX:GPL-3.0-or-later"]

[permissions]
files = "Writes the prop's GLB into the project's assets/props folder"
